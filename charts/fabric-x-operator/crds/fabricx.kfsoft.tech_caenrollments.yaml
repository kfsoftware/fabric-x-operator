---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: caenrollments.fabricx.kfsoft.tech
spec:
  group: fabricx.kfsoft.tech
  names:
    kind: CAEnrollment
    listKind: CAEnrollmentList
    plural: caenrollments
    shortNames:
    - caenroll
    singular: caenrollment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.caName
      name: CA
      type: string
    - jsonPath: .spec.enrollID
      name: EnrollID
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CAEnrollment is the Schema for the caenrollments API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CAEnrollmentSpec defines the desired state of CAEnrollment
            properties:
              affiliation:
                description: Affiliation for the identity (optional)
                type: string
              attrs:
                description: Attributes for the identity (optional)
                items:
                  description: CAEnrollmentAttr defines an attribute for a CA enrollment
                  properties:
                    ecert:
                      default: false
                      description: Whether this attribute should be included in the
                        enrollment certificate (ECert)
                      type: boolean
                    name:
                      description: Name of the attribute
                      type: string
                    value:
                      description: Value of the attribute
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              caName:
                description: Name of the Fabric CA instance to use for enrollment
                type: string
              caNamespace:
                description: Namespace of the Fabric CA instance
                type: string
              enrollID:
                description: Enrollment ID (username) for this identity
                type: string
              enrollSecretRef:
                description: Enrollment secret (password) reference from a secret
                properties:
                  key:
                    description: Key in the secret
                    type: string
                  name:
                    description: Name of the secret
                    type: string
                required:
                - key
                - name
                type: object
              maxEnrollments:
                default: 0
                description: Maximum number of enrollments (0 for unlimited)
                type: integer
              outputSecretName:
                description: Output secret name where the private key and certificate
                  will be stored
                type: string
              tls:
                description: TLS configuration for connecting to Fabric CA
                properties:
                  caCertRef:
                    description: TLS CA certificate reference from a secret
                    properties:
                      key:
                        description: Key in the secret
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  insecure:
                    default: false
                    description: Skip TLS certificate verification
                    type: boolean
                type: object
              type:
                description: Type of identity (e.g., "client", "peer", "orderer",
                  "admin")
                enum:
                - client
                - peer
                - orderer
                - admin
                type: string
            required:
            - caName
            - caNamespace
            - enrollID
            - enrollSecretRef
            - outputSecretName
            - type
            type: object
          status:
            description: CAEnrollmentStatus defines the observed state of CAEnrollment
            properties:
              certificateExpiry:
                description: Certificate expiration time
                format: date-time
                type: string
              conditions:
                description: Conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              enrollmentCount:
                description: Number of times this identity has been enrolled
                type: integer
              enrollmentTime:
                description: Enrollment timestamp
                format: date-time
                type: string
              message:
                description: Message describing the current state
                type: string
              status:
                description: Status of the identity enrollment
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
