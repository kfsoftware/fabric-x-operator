---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: geneses.fabricx.kfsoft.tech
spec:
  group: fabricx.kfsoft.tech
  names:
    kind: Genesis
    listKind: GenesisList
    plural: geneses
    shortNames:
    - genesis
    singular: genesis
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Genesis is the Schema for the genesis API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GenesisSpec defines the desired state of Genesis.
            properties:
              applicationOrgs:
                description: Application organizations (can be internal or external)
                items:
                  description: ApplicationOrganization represents an application organization
                  properties:
                    adminCertRef:
                      description: Admin certificate reference (optional)
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    mspId:
                      description: MSP ID for the organization
                      type: string
                    name:
                      description: Name of the organization
                      type: string
                    signCaCertRef:
                      description: Signing CA certificate reference
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    tlsCaCertRef:
                      description: TLS CA certificate reference
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                  required:
                  - mspId
                  - name
                  - signCaCertRef
                  - tlsCaCertRef
                  type: object
                type: array
              channelID:
                description: Channel ID for the genesis block
                type: string
              configTemplate:
                description: Config template reference
                properties:
                  configMapName:
                    description: Name of the ConfigMap containing the template
                    type: string
                  key:
                    description: Key in the ConfigMap containing the template
                    type: string
                required:
                - configMapName
                - key
                type: object
              ordererNodes:
                description: Specific orderer nodes for consensus
                items:
                  description: OrdererNode represents a specific orderer node for
                    consensus
                  properties:
                    clientTlsCertRef:
                      description: Client TLS certificate reference
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    host:
                      description: Host address of the orderer node
                      type: string
                    id:
                      description: Unique identifier for the orderer node
                      type: integer
                    identityRef:
                      description: Identity certificate reference
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    mspId:
                      description: MSP ID of the organization this orderer belongs
                        to
                      type: string
                    port:
                      description: Port number for the orderer node
                      type: integer
                    serverTlsCertRef:
                      description: Server TLS certificate reference
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                  required:
                  - clientTlsCertRef
                  - host
                  - id
                  - identityRef
                  - mspId
                  - port
                  - serverTlsCertRef
                  type: object
                type: array
              ordererOrganizations:
                description: OrdererOrganizations (with certificates stored in secrets)
                items:
                  description: OrdererOrganization represents an organization with
                    certificates stored in secrets
                  properties:
                    adminCertRef:
                      description: Admin certificate reference (optional)
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    mspId:
                      description: MSP ID for the organization
                      type: string
                    name:
                      description: Name of the organization
                      type: string
                    signCaCertRef:
                      description: Signing CA certificate reference
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    tlsCaCertRef:
                      description: TLS CA certificate reference
                      properties:
                        key:
                          description: Secret key
                          type: string
                        name:
                          description: Secret name
                          type: string
                        namespace:
                          description: Namespace of the secret
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                  required:
                  - mspId
                  - name
                  - signCaCertRef
                  - tlsCaCertRef
                  type: object
                type: array
              output:
                description: Output configuration
                properties:
                  blockKey:
                    description: Key in the Secret to store the genesis block
                    type: string
                  secretName:
                    description: Name of the Secret to store the genesis block
                    type: string
                required:
                - blockKey
                - secretName
                type: object
            required:
            - channelID
            - configTemplate
            - output
            type: object
          status:
            description: GenesisStatus defines the observed state of Genesis.
            properties:
              conditions:
                description: Conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message describing the current state
                type: string
              status:
                description: Status of the genesis block generation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
