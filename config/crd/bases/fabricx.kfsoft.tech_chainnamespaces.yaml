---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: chainnamespaces.fabricx.kfsoft.tech
spec:
  group: fabricx.kfsoft.tech
  names:
    kind: ChainNamespace
    listKind: ChainNamespaceList
    plural: chainnamespaces
    shortNames:
    - cns
    singular: chainnamespace
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.message
      name: Message
      priority: 1
      type: string
    - jsonPath: .spec.mspID
      name: MSPID
      type: string
    - jsonPath: .spec.channel
      name: Channel
      type: string
    - jsonPath: .status.txID
      name: TxID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChainNamespace is the Schema for the chainnamespaces API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NamespaceSpec defines the desired state of Namespace.
            properties:
              caCert:
                description: CACert is the reference to the CA certificate
                properties:
                  key:
                    description: Key in the secret
                    type: string
                  name:
                    description: Name of the secret
                    type: string
                  namespace:
                    description: Namespace of the secret
                    type: string
                required:
                - key
                - name
                - namespace
                type: object
              channel:
                description: Channel is the channel name to deploy the namespace to
                type: string
              identity:
                description: Identity references the MSP identity to use for signing
                properties:
                  key:
                    description: Key in the secret
                    type: string
                  name:
                    description: Name of the secret
                    type: string
                  namespace:
                    description: Namespace of the secret
                    type: string
                required:
                - key
                - name
                - namespace
                type: object
              mspID:
                description: MSPID is the MSP identifier
                type: string
              name:
                description: Name is the namespace ID
                type: string
              orderer:
                description: Orderer endpoint to broadcast the namespace transaction
                type: string
              verificationKeyPath:
                description: VerificationKeyPath is optional path to verification
                  key (if not using default MSP signer)
                type: string
              version:
                default: -1
                description: Version is the namespace version (use -1 for new namespace,
                  >= 0 for updates)
                type: integer
            required:
            - caCert
            - identity
            - mspID
            - name
            - orderer
            type: object
          status:
            description: NamespaceStatus defines the observed state of Namespace.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the namespace's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message provides details about the status
                type: string
              status:
                description: Status of the namespace deployment
                type: string
              txID:
                description: TxID is the transaction ID of the deployed namespace
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
