apiVersion: fabricx.kfsoft.tech/v1alpha1
kind: OrdererBatcher
metadata:
  labels:
    app.kubernetes.io/name: fabric-x-operator
    app.kubernetes.io/managed-by: kustomize
  name: ordererbatcher-sample
  namespace: default
spec:
  deploymentMode: "deploy"
  mspid: "OrdererMSP"
  partyID: 1
  shardID: 1
  replicas: 2
  
  # Storage configuration (optional - uses cluster default if not specified)
  # storageClassName: ""  # Uses cluster default storage class
  pvcAccessModes:
    - "ReadWriteOnce"
  pvcStorageSize: "20Gi"
  
  # Genesis block configuration
  genesis:
    secretName: "fabricx-shared-genesis"
    secretKey: "genesis.block"
    secretNamespace: "default"
  
  # Certificate configuration
  certificates:
    cahost: "test-ca2.default"
    caport: 7054
    catls:
      secretRef:
        key: "tls.crt"
        name: "test-ca2-tls-crypto"
        namespace: "default"
    enrollid: "admin"
    enrollsecret: "adminpw"
  
  # Ingress configuration (optional)
  ingress:
    istio:
      hosts:
        - "batcher-1-org1.localho.st"
      ingressGateway: "istio-ingressgateway"
      port: 443
  
  # Environment variables
  env:
    - name: "BATCHER_TIMEOUT"
      value: "2s"
    - name: "BATCHER_PREFERRED_MAX_BYTES"
      value: "2097152"
  
  # Pod labels
  podLabels:
    fabric-x/batcher-type: "high-throughput"
    fabric-x/shard-id: "1"
  
  # Resources
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
